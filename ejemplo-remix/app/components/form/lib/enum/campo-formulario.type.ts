export enum CampoFormularioType {
    Text = 'text',
    Password = 'password',
    Email = 'email',
    Url = 'url',
    Tel = 'tel'
    // date = 'date',
    // textarea = 'textarea',
    // select = 'select',
    // number = 'number',
    // mask = 'mask',
    // autoComplete = 'autoComplete',
    // file = 'file',
    // inputNumber = 'inputNumber',
    // separator = 'separator',
}